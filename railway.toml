[build]
builder = "nixpacks"
buildCommand = "pnpm install && pnpm --filter @finpilot/shared build && pnpm --filter @finpilot/api build"

[deploy]
startCommand = "node apps/api/dist/main.js"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
